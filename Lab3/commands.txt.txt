# Commands used in Lab 3

# Open the file using Vim
vim editing_final_lab.txt

# Enter Visual Mode in Vim
# Press `v`, highlight the first column, and delete the last 7 characters on the first line with `x`.

# Preserve the first four characters of the first column
# Highlight the remaining characters using Visual Mode and delete with `d`.

# Open the file using Nano
nano editing_final_lab.txt

# Edit the required parts and save with `Ctrl+O`, then exit with `Ctrl+X`.
